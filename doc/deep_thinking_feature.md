# 深度思考功能说明文档

## 概述

深度思考模式是一个多阶段推理系统,能够将复杂问题拆解为多个子任务,逐步分析并整合结果,最终生成高质量的答案。

## 功能特点

### 1. 四阶段推理流程

```
用户问题 → 规划(Plan) → 逐个分析(Solve) → 整合(Synthesize) → 审查(Review) → 最终答案
```

- **Plan (规划阶段)**: 理解问题,拆解为3-6个子任务
- **Solve (分析阶段)**: 对每个子任务进行深入分析
- **Synthesize (整合阶段)**: 综合所有子任务结论,生成最终答案
- **Review (审查阶段)**: 可选,对答案进行质量检查

### 2. 可配置选项

- **启用深度思考**: 开关深度思考模式
- **启用自我审查**: 对最终答案进行质量审查
- **显示思考过程**: 展示详细的推理步骤
- **最大子任务数**: 控制问题拆解的粒度 (3-8个)

## 使用方法

### 通过 Gradio UI 使用

1. 启动应用:
   ```bash
   python main.py
   ```

2. 在左侧控制面板找到 **"🧠 深度思考模式"** 区域

3. 勾选 **"启用深度思考"** 复选框

4. (可选) 点击 **"高级选项"** 调整配置:
    - 启用/禁用自我审查
    - 控制是否显示思考过程
    - 调整最大子任务数量

5. 输入您的问题并发送

### 通过 Python 代码使用

```python
from src.api_service import api_service
from src.deep_think import DeepThinkOrchestrator, format_deep_think_result

# 创建深度思考编排器
orchestrator = DeepThinkOrchestrator(
    api_service=api_service,
    model="qwen-3-235b-a22b-thinking-2507",  # 或其他模型
    max_subtasks=6,
    enable_review=True,
    verbose=True
)

# 执行深度思考
question = "什么是人工智能?请从历史、技术和应用三个角度分析。"
result = orchestrator.run(question)

# 格式化输出
formatted_output = format_deep_think_result(result, include_process=True)
print(formatted_output)

# 访问详细结果
print(f"子任务数量: {len(result.subtask_results)}")
print(f"LLM调用次数: {result.total_llm_calls}")
if result.review:
    print(f"质量评分: {result.review.overall_quality_score:.2%}")
```

## 适用场景

深度思考模式特别适合以下类型的问题:

### ✅ 推荐使用

1. **复杂分析问题**
    - "分析XX公司的商业模式优劣势"
    - "比较不同编程范式的特点"

2. **多角度综合问题**
    - "从经济、社会、技术角度分析XX现象"
    - "评估某个技术方案的可行性"

3. **需要结构化思考的问题**
    - "如何设计一个XX系统?"
    - "制定一个XX计划"

4. **研究型问题**
    - "XX领域的发展历程和未来趋势"
    - "深入解析XX概念"

### ❌ 不推荐使用

1. **简单事实查询**
    - "Python的版本号是多少?"
    - "什么是HTTP?"

2. **快速问答**
    - 需要即时响应的简单问题

3. **创意生成**
    - 写诗、讲故事等创造性任务

## 工作原理

### 数据流

```python
# 1. 用户输入
question = "如何提高编程技能?"

# 2. Plan 阶段 - 生成结构化规划
Plan {
    clarified_question: "如何系统性地提升编程技能?",
    subtasks: [
        {id: 1, description: "基础知识学习路径"},
        {id: 2, description: "实践项目建议"},
        {id: 3, description: "持续进步的方法"}
    ],
    reasoning_approach: "从理论学习、实践应用、持续改进三个维度分析"
}

# 3. Solve 阶段 - 逐个分析子任务
SubtaskResult[0] {
    subtask_id: 1,
    analysis: "详细的基础知识学习分析...",
    intermediate_conclusion: "应从数据结构、算法、设计模式入手",
    confidence: 0.9
}

# 4. Synthesize 阶段 - 整合所有结论
final_answer = """
# 如何提高编程技能

## 1. 基础知识学习
...

## 2. 实践项目
...

## 3. 持续改进
...
"""

# 5. Review 阶段 - 质量审查
ReviewResult {
    issues_found: ["建议增加具体的学习资源"],
    improvement_suggestions: ["可以添加时间规划建议"],
    overall_quality_score: 0.85
}
```

### 关键组件

1. **DeepThinkOrchestrator**: 核心编排器
    - 管理整个推理流程
    - 协调各个阶段的执行

2. **PromptTemplates**: Prompt 模板
    - 为每个阶段设计专门的提示词
    - 确保输出结构化的 JSON

3. **数据类** (dataclass):
    - `Plan`: 规划结果
    - `SubtaskResult`: 子任务分析结果
    - `DeepThinkResult`: 完整的思考结果
    - `ReviewResult`: 审查结果

## 扩展性设计

### 预留的工具调用接口

当前版本仅使用模型自身知识,但已预留外部工具调用能力:

```python
@dataclass
class SubtaskResult:
    # ...现有字段...
    needs_external_info: bool = False  # 标记是否需要外部信息
    suggested_tools: List[str] = field(default_factory=list)  # 建议的工具
```

未来可以轻松集成:

- 🔍 **搜索引擎**: 获取最新信息
- 📚 **RAG系统**: 从知识库检索
- 💻 **代码执行**: 运行代码验证
- 🌐 **API调用**: 查询实时数据

### 扩展示例

```python
# 未来扩展: 在 _solve_subtask 中集成工具
def _solve_subtask(self, subtask, original_question, previous_results):
    result = # ... 现有逻辑 ...

    # 检查是否需要外部信息
    if result.needs_external_info:
        for tool in result.suggested_tools:
            if tool == "search":
                # 调用搜索API
                search_results = self.search_engine.search(subtask.description)
                # 使用搜索结果增强分析
                enhanced_result = self._enhance_with_search(result, search_results)
                return enhanced_result

    return result
```

## 测试

运行测试脚本验证功能:

```bash
# 运行所有测试
python tests/test_deep_think.py

# 运行特定测试
python tests/test_deep_think.py --test basic      # 基础功能测试
python tests/test_deep_think.py --test no-review  # 无审查模式测试
python tests/test_deep_think.py --test format     # 格式化测试
```

## 性能考虑

### LLM 调用次数

对于一个典型的深度思考会话:

- Plan 阶段: 1 次调用
- Solve 阶段: N 次调用 (N = 子任务数,默认3-6)
- Synthesize 阶段: 1 次调用
- Review 阶段: 1 次调用 (如果启用)

**总计**: 约 5-9 次 LLM 调用

### 响应时间

根据模型速度,完整的深度思考流程通常需要:

- **快速模型** (如 Llama-3.3-70b): 30-60 秒
- **中等模型** (如 Qwen-235b): 60-120 秒
- **大型模型** (如 GPT-4): 120-180 秒

### 成本估算

以 Cerebras (最快最便宜) 为例:

- 每次深度思考: ~8 次调用
- 假设每次约 1000 tokens 输入 + 500 tokens 输出
- 总 token: ~12,000 tokens
- 成本: < $0.01 (实际成本取决于提供商定价)

## 最佳实践

### 1. 选择合适的模型

- **推理密集型问题**: 使用 `qwen-3-235b-a22b-thinking-2507` 或 `deepseek-chat`
- **快速原型验证**: 使用 `llama-3.3-70b` (Cerebras)
- **高质量输出**: 使用 `gpt-4` 或 `gpt-4o`

### 2. 调整子任务数量

- **简单问题**: 3-4 个子任务
- **中等复杂度**: 5-6 个子任务
- **高度复杂**: 6-8 个子任务

### 3. 何时启用审查

- ✅ **启用审查**: 重要决策、学术分析、专业建议
- ❌ **禁用审查**: 探索性问题、快速验证、资源受限

### 4. 问题表述技巧

- **清晰**: "分析X的优缺点" ✅ vs "X怎么样?" ❌
- **具体**: "比较Python和Java的性能差异" ✅ vs "比较编程语言" ❌
- **结构化**: "从A、B、C三个角度分析X" ✅

## 故障排查

### 问题 1: JSON 解析失败

**症状**: 日志显示 "JSON解析失败"

**原因**: 模型返回的不是有效的 JSON 格式

**解决**:

- 已内置多重容错机制
- 会自动使用原始响应作为后备
- 如果频繁发生,考虑更换模型

### 问题 2: 响应时间过长

**症状**: 等待时间超过 3 分钟

**原因**: 模型速度慢或网络延迟

**解决**:

- 减少 `max_subtasks` 数量
- 禁用 `enable_review`
- 切换到更快的模型 (如 Cerebras 提供的模型)

### 问题 3: 答案质量不理想

**症状**: 最终答案不够深入或偏离主题

**原因**: 问题表述不清或子任务拆解不合理

**解决**:

- 重新表述问题,更加具体
- 增加子任务数量
- 启用审查模式
- 尝试不同的模型

## 未来改进方向

1. **异步执行**: 支持 async/await,提高并发性能
2. **流式输出**: 实时展示每个阶段的进展
3. **工具集成**: 连接搜索引擎、数据库、代码执行环境
4. **多模态支持**: 处理图片、文档等非文本输入
5. **缓存优化**: 缓存中间结果,避免重复计算
6. **可视化**: 展示思考过程的流程图
7. **对话式深度思考**: 在深度思考过程中与用户交互

## 技术架构

```
┌─────────────────────────────────────────────┐
│           Gradio UI (main.py)               │
│  ┌────────────┐  ┌──────────────────────┐   │
│  │ 控制面板   │  │  深度思考配置        │   │
│  │ - 模型选择 │  │  - 启用开关          │   │
│  │ - 提供商   │  │  - 审查选项          │   │
│  └────────────┘  │  - 子任务数          │   │
│                  └──────────────────────┘   │
└──────────────┬──────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────┐
│    DeepThinkOrchestrator (deep_think.py)    │
│                                             │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  │
│  │   Plan   │→ │  Solve   │→ │Synthesize│  │
│  └──────────┘  └──────────┘  └──────────┘  │
│                      ↓                      │
│                 ┌──────────┐                │
│                 │  Review  │                │
│                 └──────────┘                │
└──────────────┬──────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────┐
│  MultiProviderAPIService (api_service.py)   │
│                                             │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐    │
│  │Cerebras  │ │DeepSeek  │ │ OpenAI   │    │
│  └──────────┘ └──────────┘ └──────────┘    │
└─────────────────────────────────────────────┘
```

## 许可与贡献

本功能是 SimpleLLMFront 项目的一部分,遵循项目许可证。

欢迎贡献改进建议和代码!
